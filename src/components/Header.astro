---
---
const navItems = [
  { linkText: 'Inicio', href: '/' },
  { linkText: 'Planes', href: '/planes' },
  { linkText: 'Demo', href: '/demo' },
  { linkText: 'Contacto', href: '/contacto' }
];
---

<nav class="flex flex-wrap items-center justify-between gap-4 pt-6 pb-4 sm:pt-8 sm:pb-6 text-white">
  <!-- LOGO -->
  <a href="/" class="flex items-center gap-2 text-xl font-bold hover:opacity-90 transition">
    <img src="/favicon.svg" alt="MarIAtech logo" class="h-7 w-7" />
    🌊 MarIAtech
  </a>

  <!-- NAVIGATION MENU -->
  { !!navItems?.length && (
    <ul class="flex flex-wrap gap-x-6 gap-y-2 text-sm sm:text-base">
      {navItems.map((item) => (
        <li>
          <a
            href={item.href}
            class="hover:text-blue-300 transition"
          >
            {item.linkText}
          </a>
        </li>
      ))}
    </ul>
  )}
</nav>

